<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hagitai">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name=".HagitApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ai_logo"
        android:roundIcon="@mipmap/ai_logo_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HagitAI">

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.HagitAI.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".LoginActivity"  android:screenOrientation="portrait" />
        <activity android:name=".SignupActivity" android:screenOrientation="portrait" />
        <activity android:name=".ForgotPasswordActivity" android:screenOrientation="portrait" />

        <!-- Home -->
        <activity android:name=".HomeActivity"   android:screenOrientation="portrait" />

        <!-- Modes -->
        <activity android:name=".FindingMatchActivity" android:screenOrientation="portrait" />
        <activity android:name=".MatchFoundActivity" />
        <activity android:name=".PracticeActivity"     android:screenOrientation="portrait" />
        <activity android:name=".RandomtopicActivity"  android:screenOrientation="portrait" />
        <activity android:name=".GeneratingTopicActivity" android:screenOrientation="portrait" />
        <activity android:name=".TopicResultActivity" android:screenOrientation="portrait" />

        <!-- Debate -->
        <activity android:name=".DebateActivity"       android:screenOrientation="portrait" />
        <activity android:name=".DebateRoomActivity"   android:screenOrientation="portrait" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".DebateResultActivity" android:screenOrientation="portrait" />

        <!-- Other -->
        <activity android:name=".LeaderboardActivity"  android:screenOrientation="portrait" />
        <activity android:name=".ProfileActivity"      android:screenOrientation="portrait" />
        <activity android:name=".RecentHistoryActivity" android:screenOrientation="portrait" />

    </application>
</manifest>